#include <stdio.h>

/**
 * main - prints the alphabet
 *
 * Return: 
 */
int main(void)
{
	int letter = 0;
	while (letter != 25)
	{
		int currentLetter = 97 + leter;
		putchar(currentLetter);
	}
	putchar('\n');
}
