#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * main - explanation
 *
 * return: 0
 */
char *_strdup(char *str)
{
	if (str == NULL)
	{
		return (NULL);
	}
	char *dupStr =  (char *)malloc(sizeof(str) * size);
	
	return (array);
}
